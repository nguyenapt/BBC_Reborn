VOA iOS — credentials còn thiếu (điền trước khi ship TestFlight)
===============================================================================

1) AdMob iOS — DONE
   - App ID: ca-app-pub-2189112136936277~2745686014
   - Banner / Interstitial / App open / Rewarded / Native: đã gắn trong Dart + Info.plist

2) App Store Connect Apple ID — DONE
   - kIosAppStoreId = 6793363882 (Learning English - VOA)

3) Google Sign-In OAuth (Firebase) — DONE
   - GoogleService-Info.plist đã có CLIENT_ID / REVERSED_CLIENT_ID
   - Info.plist đã có CFBundleURLTypes URL scheme
   - firebase_options.dart iOS đã sync apiKey + appId mới

Còn lại (console, không phải code):
- APNs Auth Key upload lên Firebase Cloud Messaging
- App Check → App Attest cho app iOS
- Authentication: bật Google + Apple nếu chưa

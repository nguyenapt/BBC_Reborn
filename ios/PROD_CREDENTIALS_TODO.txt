VOA iOS — credentials còn thiếu (điền trước khi ship TestFlight production ads / rate)
===============================================================================

1) AdMob iOS App ID
   - File: ios/Runner/Info.plist → GADApplicationIdentifier
   - Hiện tại: Google test ID ca-app-pub-3940256099942544~1458002511
   - Cần: ca-app-pub-…~… (app VOA trên AdMob)

2) AdMob iOS ad unit IDs
   - lib/services/admob_service.dart → _prod*AdUnitIdIOS (banner, interstitial, app-open, rewarded)
   - lib/widgets/native_ad_widget.dart → _productionAdUnitIdIOS
   - lib/widgets/transcript_native_ad_widget.dart → _productionAdUnitIdIOS
   - Hiện tại: đang dùng Google test units (có TODO(iOS) trong code)

3) App Store Connect Apple ID (số)
   - lib/config/store_links.dart → kIosAppStoreId
   - Hiện tại: '' (trống)

4) Google Sign-In OAuth (Firebase)
   - Tải lại ios/Runner/GoogleService-Info.plist từ Firebase project
     voa-learning-english-c75fe (bật Google Sign-In / OAuth client iOS)
   - Thêm CFBundleURLTypes + REVERSED_CLIENT_ID vào ios/Runner/Info.plist
   - Plist hiện tại thiếu CLIENT_ID / REVERSED_CLIENT_ID

Sau khi điền: bỏ các TODO(iOS) liên quan trong Dart/plist.
